---
title: "Design Pattern"
description: ""
tags: ["Knowledge","CS","Design Pattern"]
date: "2023-07-08"
thumbnail: "/markdown-memo/legacy/images/1bda935d-5c77-442f-af97-93e08a8811db.png"
---

<!-- Table of Contents -->


# 개요

디자인 패턴은 소프트웨어 디자인의 일반적인 문제들에 대해 검증된 해결책의 모음집이다. 

디자인 패턴은 다른 사람들과 협업을 할 때 사용할 수 있는 공통 언어를 정의함으로써 목표를 달성하는데 효율적인 의사소통이 가능하게 한다.

# 생성 패턴

## Abstract Factory

추상 팩토리는 관련 객체들의 구상 클래스들을 지정하지 않고도 관련 객체들의 모음을 생성할 수 있도록 하는 생성패턴이다.


예를 들어 우리가 RPG 게임에서 직업을 고른다고 생각해보면 다음과 같다.

1. 관련 직업들로 구성된 직업군
1. 해당 직업의 여러 가지 변형
또한, 게임은 언제든지 업데이트가 가능하기 때문에 새로운 능력을 추가할 때 마다 기존 코드를 변경해야 하는 번거로움을 피해야 한다.

이러한 문제를 해결하기 위해 추상 팩토리 패턴을 사용한다.

1. 추상 팩토리 패턴은 각 직업 군에 해당하는 개별적인 인터페이스를 명시적으로 선언한다. 
1. 추상 팩토리 패턴의 다음 단계는 추상 팩토리 패턴을 선언한다.
1. 다음은 직업의 변형을 다룬다. 
## Prototype

프로토타입은 코드를 그들의 클래스에 의존하지 않고 기존 객체를 복사할 수 있도록 하는 생성 디자인 패턴이다.


객체에 대한 복사본을 만들기 위해 **원본 객체의 모든 필드들을 새 객체에 복사**해야 하는데 여기에는 큰 문제가 있다.

1. 객체의 필드 중 일부가 비공개 일 수 있다.
1. 코드가 해당 클래스에 의존하게 된다.

이러한 문제를 해결하기 위해 프로토타입 패턴이 사용된다.

프로토타입 패턴은 복제되는 객체들에 복제 프로세스가 위임된다. 따라서 복제를 지원하는 모든 객체에 대한 공통 인터페이스를 선언한다.

해당 인터페이스에는 단일 `clone` 메서드만 포함된다.


---


`clone` 메서드의 구현은 모든 클래스에서 매우 유사하다.

1. 현재 클래스의 객체를 만든다
1. 이전 객체의 모든 필드 값을 새 객체로 전달한다.

## Builder

빌더는 복잡한 객체들을 단계 별로 생성할 수 있도록 하는 생성 디자인 패턴이다. 이 패턴을 사용하면 같은 제작 코드를 사용하여 객체의 다양한 유형들과 표현을 제작할 수 있다.

필드가 많고 중첩된 객체들을 초기화해야 한다면 일반적인 초기화 코드는 굉장히 매개변수가 많을 것이다.

빌더 패턴은 클래스에서 객체 생성 코드를 추출하여 `builders` 라는 별도의 객체들로 이동하도록 제안한다.

![](/markdown-memo/legacy/images/1bda935d-5c77-442f-af97-93e08a8811db.png)


더 나아가서 빌더 단계에 대한 일련의 호출을 디렉터라는 별도의 클래스로 추출할 수 있다. 

디렉터 클래스는 제작 단계들을 실행하는 순서를 정의하는 반면 빌더는 이러한 단계들에 대한 구현을 제공한다.


## Singleton

싱글턴은 클래스에 인스턴스가 하나만 있도록 하면서 이 인스턴스에 대한 전역 접근(액세스) 지점을 제공하는 생성 디자인 패턴이다.

싱글턴 패턴이 사용되는 경우는 모든 클라이언트가 사용할 수 있는 단일 인스턴스만이 존재해야 되는 상황, 예를 들어 프로그램의 다른 부분들에서 공유되는 단일 데이터베이스 객체같은 경우에서 사용된다.


# 구조 패턴

## Adapter

<!-- Unknown block type: synced_block -->

![재밌는 예시](/markdown-memo/legacy/images/79620ee5-113d-4c24-8123-537c03783255.png)

어댑터 패턴이 작동하는 방식은 다음과 같다:

1. 기존에 있는 객체 중 하나와 호환되는 인터페이스를 받는다
1. 해당 인터페이스를 사용하면 기존 객체는 어댑터의 메서드들을 안전하게 호출할 수 있다
1. 호출을 수신하면 어댑터는 이 요청을 두 번째 객체에 해당 객체가 예상하는 형식과 순서대로 전달한다.

구현방법은 다음과 같다:

1. 호환되지 않는 인터페이스가 있는 클래스가 최소 두 개 이상 있는지 확인한다.
1. 클라이언트 인터페이스를 선언하고 클라이언트들이 서비스와 통신하는 방법을 기술한다.
1. 어댑터 클래스를 생성한 후 클라이언트 인터페이스를 따르게 한다. 일단은 모든 메서드를 비워 둔다.
1. 서비스 객체에 참조를 저장하기 위해 어댑터 클래스에 필드를 추가한다. 일반적으로 생성자를 통해 이 필드를 초기화 하는 것이지만, 때때로 어댑터의 메서드를 호출할 떄는 이 필드를 어댑터에 전달하는 것이 더 편리하기 하다.
1. 클라이언트 인터페이스의 모든 메서드를 어댑터 클래스에서 하나씩 구현한다. **어댑터는 인터페이스 또는 데이터 형식 변환만 처리해야 하며, 실제 작업의 대부분을 서비스 객체에 위임해야 한다.**
1. 클라이언트들은 클라이언트 인터페이스를 통해 어댑터를 사용해야 한다. 이렇게 함으로써 클라이언트 코드에 영향을 주지 않고 어댑터들을 변경하거나 확장할 수 있다.

## Bridge

**브리지**는 큰 클래스 또는 밀접하게 관련된 클래스들의 집합을 두 개의 개별 계층구조(추상화 및 구현)로 나눈 후 각각 독립적으로 개발할 수 있도록 하는 구조 디자인 패턴이다.

![](/markdown-memo/legacy/images/1283a395-1329-4573-9514-17c446b4dbf1.png)

추상화는 **상위 수준의 제어 논리**를 제공하며, 구현 객체에 의존해 실제 하위 수준 작업들을 수행한다.

구현은 모든 **구상 구현들에 공통적인 인터페이스**를 선언하며, 추상화는 **여기에 선언된 메서드**들을 통해서만 구현 객체와 소통할 수 있다.

추상화는 구현과 같은 메서드들을 나열할 수 있지만 보통은 구현이 선언한 다양한 원시 작업들에 의존하는 **몇 가지 복잡한 행동**들을 선언한다.

구상 구현들에는 플랫폼 별 맞춤형 코드가 포함된다

정제된 추상화들은 제어 논리의 변형들을 제공한다. 그들은 그들의 부모처럼 일반 구현 인터페이스를 통해 다른 구현들과 작업한다.

일반적으로 클라이언트는 추상화와 작업하는데만 관심이 있다. 그러나 추상화 객체를 구현 객체들 중 하나와 연결하는 것도 클라이언트의 역할이다.

## Composite

복합체 패턴은 객체들을 트리 구조들로 구성한 후, 이러한 구조들과 개별 객체들처럼 작업할 수 있도록 하는 구조 패턴이다.

> 💡 복합체 패턴은 앱의 핵심 모델이 트리로 표현될 수 있을 때만 사용해야 한다.

![](/markdown-memo/legacy/images/a9f5f181-7e44-4e60-86f4-b69c5760e5dc.png)

컴포넌트 인터페이스는 트리의 단순 요소들과 복잡한 요소들 모두에 공통적인 작업을 설명한다.

잎은 트리의 기본 요소이며 하위 요소가 없다.

컨테이너(일명 복합체)는 하위 요소들(잎 또는 기타 컨테이너)이 있는 요소이다. 컨테이너는 자녀들의 구상 클래스들을 알지 못하며, 컴포넌트 인터페이스를 통해서만 모든 하위 요소들과 함께 작동한다.

클라이언트는 컴포넌트 인터페이스를 통해 모든 요소들과 작동한다. 그 결과 클라이언트는 트리의 단순 요소들 또는 복잡한 요소들 모두에 대해 같은 방식으로 작업할 수 있다.

## Decorator

데코레이터는 객체들을 새로운 행동들을 포함한 특수 래퍼 객체들 내에 넣어서 위 행동들을 해당 객체에 연결시키는 구조적 디자인 패턴이다.

![](/markdown-memo/legacy/images/365afd8e-b11f-44d5-8cd2-ea989f1d414f.png)

컴포넌트는 래퍼들과 래핑된 객체들 **모두에 대한 공통 인터페이스**를 선언한다

구상 컴포넌트는 래핑되는 객체들의 클래스이며, 기본 행동들을 정의하고 해당 기본 행동들은 데코레이터들이 변경할 수 있다

기초 데코레이터 클래스에는 래핑된 객체를 참조하기 위한 필드가 있다.  필드의 유형은 구상 컴포넌트들과 구상 데코레이터들을 모두 포함할 수 있도록 컴포넌트 인터페이스로 선언되어야 한다. 그 후, 기초 데코레이터는 모든 작업들을 래핑된 객체에 위임한다.

구상 데코레이터들은 컴포넌트들에 동적으로 추가될 수 있는 추가 행동들을 정의한다. 그들은 기초 데코레이터의 메서드를 오버라이드하고 해당 행동을 부모 메서드를 호출하기 전이나 후에 실행한다.

클라이언트는 아래에 언급한 데코레이터들이 컴포넌트 인터페이스를 통해 모든 객체와 작동하는 한 컴포넌트들을 여러 계층의 데코레이터들로 래핑할 수 있다.

## Flyweight

플라이웨이트는 각 객체에 모든 데이터를 유지하는 대신 여러 객체들 간에 상태의 공통 부분들을 공유하여 사용할 수 있는 RAM에 더 많은 객체들을 포함할 수 있도록 하는 구조 디자인 패턴이다.

![플라이웨이트 구조](/markdown-memo/legacy/images/e3859628-6cc3-44ae-b3e2-c0cf7473972f.png)

플라이웨이트 클래스에는 여러 객체들 간에 공유할 수 있는 원래 객체의 상태의 부분이 포함된다.

콘텍스트 클래스는 공유한 상태를 포함하며, 모든 원본 객체들에서 고유하다. **콘텍스트와 플라이웨이트 객체 중 하나와 쌍을 이루면 원래 객체의 전체 상태를 나타낸다.**

일반적으로 원래 객체의 행동은 플라이웨이트 클래스에 남아있다. 이 경우 플라이웨이트의 메서드의 호출자는 공유한 상태의 적절한 부분들을 메서드의 매개변수들에 전달해야 한다. 반면에, 행동은 콘텍스트 클래스로 이동할 수 있으며, 이 클래스는 연결된 플라이웨이트를 단순히 데이터 객체로 사용할 것이다.

클라이언트는 플라이웨이트들의 공유된 상태를 저장하거나 계산한다.

플라이웨이트 팩토리는 기존 플라이웨이트들의 풀을 관리한다.


## Facade

**퍼사드** 패턴은 라이브러리에 대한, 프레임워크에 대한 또는 다른 클래스들의 복잡한 집합에 대한 단순화된 인터페이스를 제공하는 구조적 디자인 패턴이다.

![](/markdown-memo/legacy/images/c525d701-8a2e-48fa-8d09-672e9fc46ae7.png)


## Proxy

프록시는 다른 객체에 대한 대체 또는 자리표시자를 제공할 수 있는 구조 디자인 패턴이다. 프록시는 원래 객체에 대한 접근을 제어하므로, 당신의 요청이 원래 객체에 전달되기 전 또는 후에 무언가를 수행할 수 있도록 한다.

프록시 패턴은 **원래 서비스 객체와 같은 인터페이스**로 새 프록시 클래스를 생성하라고 제안한다.

그러면 프록시 객체를 원래 객체의 모든 클라이언트들에 전달하도록 앱을 업데이트 할 수 있다.

클라이언트로부터 요청을 받으면 이 프록시는 실제 서비스 객체를 생성하고 모든 작업을 이 객체에 위임한다.

![](/markdown-memo/legacy/images/7db6f1a8-a706-45bb-8e11-da7594953972.png)

**서비스 인터페이스**는 서비스의 인터페이스를 선언한다. 프록시 객체로 위장할 수 있으려면 이 인터페이스를 따라야 한다.

**서비스**는 어떤 유용한 비즈니스 로직을 제공하는 클래스이다.

**프록시** 클래스에는 서비스 객체를 가르키는 참조 필드가 있다. 프록시가 요청의 처리(예: 초기화 지연, 로깅, 액세스 제어, 캐싱 등)를 완료하면, 그 후 처리된 요청을 서비스 객체에 전달한다.

**클라이언트**는 같은 인터페이스를 통해 서비스들 및 프록시들과 함께 작동해야 한다. 그러면 서비스 객체를 기대하는 모든 코드에 프록시를 전달할 수 있기 때문이다.


# 행동 패턴

## CoR (Chain of Responsibility)

책임 연쇄 패턴은 핸들러들의 체인을 따라 요청을 전달 할 수 있게 해주는 행동 디자인 패턴이다. 핸들러는 요청을 받으면 요청을 처리할지 아니면 체인의 다음 핸들러로 전달할지를 결정한다.

![](/markdown-memo/legacy/images/fa4711ea-6883-4034-ab64-b8530fec72fe.png)

책임 연쇄 패턴은 특정 행동들을 **핸들러**라는 독립 실행형 객체들로 변환한다. 핸들러가 연쇄적으로 요청을 연쇄적으로 처리하면서 이 요청을 다음 체인으로 전달할 지 결정한다.

![CoR의 구조](/markdown-memo/legacy/images/5529b0de-0e25-4d09-8cc3-2054d8eb9475.png)

핸들러는 모든 구상 핸들러에 공통적인 인터페이스를 선언한다. 일반적으로 여기에는 요청을 처리하기 위한 단일 메서드만 포함되지만 때로는 체인의 다음 핸들러를 세팅하기 위한 다른 메서드가 있을 수도 있다.

*`BaseHandler`** *는 선택적 클래스이며 여기에 모든 핸들러 클래스들에 공통적인 상용구 코드를 넣을 수 있다.

*`ConcreteHandlers`* 에는 요청을 처리하기 위한 실제 코드가 포함되어 있다. 각 핸들러는 요청을 받으면 이 요청을 처리할지와 함께 체인을 따라 전달할지를 결정해야 한다.

*`Client`** *는 앱의 논리에 따라 체인들을 한 번만 구성하거나 동적으로 구성할 수 있다.

## Command

**커맨드**는 요청을 요청에 대한 모든 정보가 포함된 독립실행형 객체로 변환하는 행동 디자인 패턴이다. 이 변환은 다양한 요청들이 있는 메서드들을 인수화 할 수 있도록 하며, 요청의 실행을 지연 또는 대기열에 넣을 수 있도록 하고, 또 실행 취소할 수 있는 작업을 지원할 수 있도록 한다.

![](/markdown-memo/legacy/images/27eb929b-8c27-42de-897e-be1e52a61a63.png)

여러 클래스에 같은 기능이 있을 경우 코드의 복잡성이 증가할 뿐만 아니라 해당 기능에 대한 유지 보수가 어려워진다.

커맨드 패턴은 관심사 분리의 원칙을 기반으로 움직이며 클래스에 존재하는 기능을 커맨드 객체로 분리하여 클라이언트 객체와 연결 된다.

## Iterator


## Mediator

**중재자**는 객체 간의 혼란스러운 의존 관계들을 줄일 수 있는 행동 디자인 패턴입니다. 이 패턴은 객체 간의 직접 통신을 제한하고 중재자 객체를 통해서만 협력하도록 합니다.

![](/markdown-memo/legacy/images/998dbf31-7787-4412-88bb-ec11c88826f3.png)

## Memento, Snapshot

**메멘토**는 객체의 구현 세부 사항을 공개하지 않으면서 해당 객체의 이전 상태를 저장하고 복원할 수 있게 해주는 행동 디자인 패턴입니다.

![](/markdown-memo/legacy/images/ad2a17ee-ee70-4242-aaa5-a73686986dc6.png)

`Originator` 클래스는 자신의 상태에 대한 스냅샷들을 생성할 수 있으며, 필요시 스냅샷에서 자신의 상태를 복원할 수 있다.

`Memento` 는 `Originator` 의 상태의 스냅샷 역할을 하는 값 객체다. 관행적으로 `Memenro`는 불변으로 만든 후 생성자를 통해 데이터를 한 번만 전달한다.

`CareTaker`는* “언제”* 그리고* “왜”* `Originator`의 상태를 캡처해야 하는 뿐만 아니라 상태가 복원돼야 하는 시기도 알고 있다.

`CareTaker`는 `Memento`들의 스택을 저장하여 `Originator`의 기록을 추적할 수 있다.

`Originator`가 과거로 돌아가야 할 때 `CareTaker` 는 맨 위의 `Memento`를 스택에서 가져온 후 `Originator`의 복원 메서드에 전달한다.

## Observer

**옵서버** 패턴은 당신이 여러 객체에 자신이 관찰 중인 객체에 발생하는 모든 이벤트에 대하여 알리는 구독 메커니즘을 정의할 수 있도록 하는 행동 디자인 패턴이다.

![](/markdown-memo/legacy/images/0b3eb330-fda0-44bf-9f42-dd583fb60a5a.png)


## State

**상태** 패턴은 객체의 내부 상태가 변경될 때 해당 객체가 그의 행동을 변경할 수 있도록 하는 행동 디자인 패턴입니다. 객체가 행동을 변경할 때 객체가 클래스를 변경한 것처럼 보일 수 있다.

상태 머신들은 일반적으로 객체의 상태에 따라 적절한 행동들을 선택하는 **많은 조건문 (****`if`**** 또는 ****`switch`****)으로 구현**된다.

조건문들에 기반한 상태 머신의 가장 큰 약점은 클래스에 상태들과 상태에 의존하는 행동들을 추가할 수록 분명해진다.

이와 같은 코드들은 유지 관리하기가 매우 어렵다. 왜냐하면 논리를 변경하려면 **모든 메서드들의 상태 조건문들을 변경**해야 할 수 있기 때문이다.

**상태 패턴은 객체의 모든 가능한 상태들에 대해 새 클래스를 만들고 모든 상태별 행동들을 이러한 클래스로 추출할 것을 제안한다.**

![](/markdown-memo/legacy/images/0cdc3ed4-d76c-42b3-b9d4-6b6d5196e73d.png)

## Strategy

**전략** 패턴은 알고리즘들의 패밀리를 정의하고, 각 패밀리를 별도의 클래스에 넣은 후 그들의 객체들을 상호교환할 수 있도록 하는 행동 디자인 패턴이다.

전략 패턴은 특정 작업을 다양한 방식으로 수행하는 클래스를 선택한 후 모든 알고리즘을 전략들(strategies)이라는 별도의 클래스들로 추출할 것을 제안한다.

콘텍스트(context)라는 원래 클래스에는 전략 중 하나에 대한 참조를 저장하기 위한 필드가 있어야 한다. 콘텍스트는 작업을 자체적으로 실행하는 대신 연결된 전략 개체에 위임한다.

![](/markdown-memo/legacy/images/738e0449-6e6c-4828-b070-599df83c7b4a.png)

## Template Method


## Visitor

**비지터**(방문자) 패턴은 알고리즘들을 그들이 작동하는 객체들로부터 분리할 수 있도록 하는 행동 디자인 패턴이다.

![](/markdown-memo/legacy/images/9e5ee725-d7f8-4279-94cf-eaf3ab46ee48.png)

# References

[https://refactoring.guru/ko/design-patterns/catalog](https://refactoring.guru/ko/design-patterns/catalog)

