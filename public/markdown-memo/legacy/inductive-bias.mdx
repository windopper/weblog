---
title: "Inductive bias"
description: ""
tags: ["ML","Knowledge"]
date: "2023-01-22"
thumbnail: "/markdown-memo/legacy/images/2155f8ef-6c37-4571-ac2d-34424e2e4504.png"
---

# Inductive Bias

> *학습 알고리즘의 inductive bias (또한 learning bias라고도 알려짐)는 한번도 마주치지 않은 입력 값에 대한 예측을 할 때 사용하는 추가적인 가정이다.

*[https://en.wikipedia.org/wiki/Inductive_bias](https://en.wikipedia.org/wiki/Inductive_bias)

**Inductive bias는 머신러닝이나 딥러닝 모델이 한번도 보지 못한 상황에 대해 정확한 예측을 할 수 있는 능력**에서 매우 중요한 역할을 한다.

예를 들어 선형 회귀 모델을 사용하는 것은 **입력 데이터가 선형성을 띄는 것을 확인했거나, 선형성을 띈다고 가정해도 충분**하다고 할 수 있을 것인데, 이러한 것들이 Inductive bias의 대표적인 예시가 된다.

**strong inductive bias**한 모델은 **입력 데이터에 대한 편향을 강화**시켜 훈련시에 빠르게 global optimum에 수렴할 수 있도록 한다.

반면에 **weak inductive bias**한 모델은 **입력 데이터에 대한 편향을 약화**시켜 local optima에 쉽게 빠질 뿐만아니라 **초기 가중치의 무작위 값에 쉽게 영향**을 받을 수 있다.

# Relational inductive biases

![](/markdown-memo/legacy/images/2155f8ef-6c37-4571-ac2d-34424e2e4504.png)


![](/markdown-memo/legacy/images/26c04bd7-6bd5-46aa-95d7-5ef3493813fe.png)

## Fully connected layers

신경망의 유닛들과의 관계는 ***all-to-all (layer i의 모든 유닛들은 layer j의 모든 유닛과 연결됨)***** 관계이다.**

신경망의 각 유닛의 인자는 입력 데이터이며, 어떠한 ***reuse***** 와 *****isolation of information***도 없다.

전결합층에서의 **암시적인 *****inductive bias*****는 매우 약하다 (특별한 관계가 없다)**. 왜냐하면 모든 입력 유닛은 어떤 출력 유닛이든 값을 결정하는데 독립적으로 작용하기 때문이다.

## Convolutional layers

입력 값의 **엔티티들은 각각이 독립적**이지만 그들 사이의 ***relation*****는 *****sparser***하다는 특징이있다.

전결합층과 합성곱층의 차이는 **locality와 translation invariance**와 같은 중요한 relational inductive bias을 두고 있다는 것이다.

> 💡 *Locality는 이미지에서 한 점과 의미있게 연결된 점은 그 주변에 있는 점들로만 국한된다는 뜻.
Translation Invariance는 입력 이미지의 오브젝트의 위치가 바뀌어도 그 모델의 출력은 바뀌지 않는 특성을 가지고 있다는 뜻으로, 반대 개념으로 Translation Equivariance가 있다.*


합성곱 층이 *locality*와 *translation invariance*의 inductive bias를 가지고 있는 이유는 다음과 같다.

합성곱 층은 특정 크기를 가진 필터가 입력 값을 슬라이딩하며 인접 픽셀 간의 정보를 추출한다. 이는 **입력 데이터의 local 영역에 대해 spatial한 정보**를 잘 뽑아 낼 수 있기 때문에 **합성곱 레이어는 locality한 inductive bias**를 가지고 있다고 할 수 있다.

또한 합성곱 층은 입력 데이터 전체에 대해 **파라미터를 공유(*****parameter sharing*****)**하기 때문에 **translation invariance**의 특징 또한 가지게 된다.

예를 들어, 강아지 사진에 대해 **강아지가 어느 위치에 있건** 합성곱 레이어의 필터는 슬라이딩하며 강아지의 특징을 포착하여 강아지가 존재한다는 출력 값을 낼 것이다.

만약 파라미터를 공유하지 않고 각 위치마다 다른 필터를 사용한다면 오브젝트의 위치마다 처음보는 특성으로 판단하게 된다.

## Recurrent layers

순환 신경층은 해당 스텝의 hidden state를 갱신하기 위해 **스텝의 입력값과 이전 hidden state**를 인자로 받는다.

이는 **해당 스텝의 hidden state가 입력 값과 이전 hidden state와 관계가 있다**는 것을 보여주며, CNN의 translational invariance의 inductive bias를 가지고 있는 것과 비슷하게 **순환 신경층이 temporal invariance한 특성**을 가지고 있다고 볼 수 있다.

RNN는 마르코프 구조를 통해 시퀀스의 locality에 대한 편향을 가지고 있다.


# Types of Inductive Bias

## Translation Invariance

간단히 말하자면 한 이미지에 속해 있는 오브젝트의 위치가 바뀌어도 출력 값은 바뀌지 않는다.

예를 들면, 이미지를 입력 받아 분류 문제를 수행하는 모델에서 한 강아지 이미지를 입력 값으로 넣는 다면 모델은 출력 값으로 “강아지”를 내보낼 것이다.

만약 강아지의 위치를 옮긴 이미지를 입력 값으로 넣는다면 역시나 모델은 출력 값으로 “강아지”를 내보낼 것이다.

여기서 모델은 **translation invariance 한 특성을 가지고 있다**고 할 수 있다.

이미지 관련 문제를 해결하는데 주로 쓰이는 CNN 아키텍처의 경우 각 레이어를 통과한 피쳐들의 해상도를 줄일 때 풀링 층(최대, 평균 등) 을 사용하게 되는데 **풀링 층은 간단하게 위치에 관계 없이 이미지의 작은 부분을 처리하기 때문에 translation invariance한 특성**을 가지고 있다.

## Translation Equivariance

Translation Invariance와 반대되는 개념으로 이미지에 속해 있는 오브젝트의 위치가 바뀐 다면 모델의 출력 값도 바뀐다는 의미이다.

대표적으로 **합성곱 연산이 translation equivariance 특성**을 가지고 있다.


# References

[https://arxiv.org/pdf/1806.01261.pdf](https://arxiv.org/pdf/1806.01261.pdf)

[https://en.wikipedia.org/wiki/Inductive_bias](https://en.wikipedia.org/wiki/Inductive_bias)

[https://velog.io/@euisuk-chung/Inductive-Bias%EB%9E%80](https://velog.io/@euisuk-chung/Inductive-Bias%EB%9E%80)

[https://www.baeldung.com/cs/ml-inductive-bias](https://www.baeldung.com/cs/ml-inductive-bias)

[https://enfow.github.io/paper-review/graph-neural-network/2021/01/11/relational_inductive_biases_deep_learning_and_graph_netowrks/](https://enfow.github.io/paper-review/graph-neural-network/2021/01/11/relational_inductive_biases_deep_learning_and_graph_netowrks/)

[https://seoilgun.medium.com/cnn%EC%9D%98-stationarity%EC%99%80-locality-610166700979](https://seoilgun.medium.com/cnn%EC%9D%98-stationarity%EC%99%80-locality-610166700979)
[https://towardsdatascience.com/translational-invariance-vs-translational-equivariance-f9fbc8fca63a](https://towardsdatascience.com/translational-invariance-vs-translational-equivariance-f9fbc8fca63a)

